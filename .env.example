# Copy this file to .env for local Netlify CLI testing (netlify dev).
# In production, set this in the Netlify dashboard instead:
# Site settings -> Environment variables. Never commit the real key.

# GetResponse account-wide API key. Found in GetResponse under
# Menu -> Integrations & API -> API.
GETRESPONSE_API_KEY=
